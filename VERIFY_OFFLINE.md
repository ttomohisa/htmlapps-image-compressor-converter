# Offline verification
1. Run `build-standalone.bat`.
2. Disconnect the network.
3. Open `dist/index.html` with `file://`.
4. Add PNG/JPEG/WebP images and convert them.
5. Test format comparison, Base64, Web assets ZIP and batch ZIP.
6. Confirm DevTools Network shows no runtime requests.
